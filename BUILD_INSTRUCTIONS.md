# بناء بوصلة.exe — خطوات سريعة

## المتطلبات
- Python 3.10 أو 3.11 (64-bit) على ويندوز
- الاتصال بالإنترنت لتثبيت المكتبات

---

## الخطوات

### 1. تثبيت PyInstaller والمكتبات
افتحي Command Prompt داخل مجلد المشروع ثم:

```
pip install -r requirements.txt
pip install pyinstaller
```

### 2. ضعي مفتاح OpenAI
افتحي ملف `.env` وعدّلي السطر:
```
OPENAI_API_KEY=sk-مفتاحك-الحقيقي-هنا
```

### 3. بناء الـ .exe
```
pyinstaller bawsala.spec
```

انتظري 3-5 دقائق. ستجدين المخرج في:
```
dist/بوصلة/بوصلة.exe
```

### 4. التسليم
ضغّطي مجلد `dist/بوصلة/` كاملاً في ZIP وسمّيه:
```
بوصلة – مروة الكواري.zip
```

---

## ملاحظات
- الـ .exe يفتح المتصفح تلقائياً على `localhost:8501`
- رُشد (AI) يحتاج إنترنت لأنه يتصل بـ OpenAI
- بقية وظائف المنصة تعمل بدون إنترنت
